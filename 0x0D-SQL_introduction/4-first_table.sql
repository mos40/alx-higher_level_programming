-- 4-first_table.sql
-- Creates the table first_table in the specified database

USE `hbtn_0c_0`;  -- Replace 'hbtn_0c_0' with the actual database name

CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
